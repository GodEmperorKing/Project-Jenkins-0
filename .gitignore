# Local .terraform directories
**/.terraform/*

# .tfstate files (CRITICAL SECURITY RISK)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which contain sensitive data (passwords, keys)
*.tfvars
*.tfvars.json

# Ignore plan files
*tfplan*